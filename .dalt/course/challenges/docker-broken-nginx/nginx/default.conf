server {
    listen 80;
    root /var/www/html/public;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        include fastcgi_params;

        # TODO: Forward PHP requests to the php-fpm container.
        # The app service in docker-compose.yml is named "app".
        # PHP-FPM listens on port 9000.
        # Directive: fastcgi_pass

        # TODO: Set the fastcgi_param that tells PHP-FPM which file to run.
        # The parameter name and the path to use are both documented in the lesson.
    }
}
